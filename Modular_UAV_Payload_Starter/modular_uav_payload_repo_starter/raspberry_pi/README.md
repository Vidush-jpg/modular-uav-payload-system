# Raspberry Pi Payload Manager

This directory is reserved for the main companion-computer implementation.

The production design will eventually contain the Raspberry Pi PDS decoder,
payload discovery service, transport adapters, payload drivers, streaming, and
system integration. Arduino proof-of-concept code belongs under
`prototypes/arduino_uno_r4`, not here.
